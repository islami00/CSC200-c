#include <stdio.h>
// scanf and reading input
// c is declarative hence everything is a function.
// or raher, c is

int main(){
    int number;
    int a = 4;
    int b = 5;
    int sum;
    scanf("%d",&a);
    scanf("%d", &b);
    sum = a + b;
    printf("the sum =%d",sum);
}