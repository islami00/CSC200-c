#include <stdio.h>

int main()

{
    // declare sum,x;
    int sum,x;
    // initialize sum,x to 0,1 respectively.
    x=1;
    sum=0;
    // increment sum by x
    sum+= x;
    // print
    printf("The sum is %d", sum);

    return 0;
}