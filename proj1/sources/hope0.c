#include<stdio.h>
#include<conio.h> //these two are important dependencies for us to use the following functions

int main(){ 
    int clscr(); //clears previous output
    printf("\nsighmantfisthis");
    getch(); //waits untl keystroke before terminating program
    return 0;
    }//basic program to print a simple string on output device, here specified in task as windows shell

//lesson learned: almost every fucntion requires int return